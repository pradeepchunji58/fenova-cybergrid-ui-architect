import {
  initialSiteSettings,
  initialHeroSlides,
  initialProjects,
  initialServices,
  initialLocations,
  initialClients,
  initialTestimonials,
  initialLeaders,
  initialCareers,
  initialBrochures,
  initialMedia,
  initialQuotations,
  initialContacts,
  initialApplications,
} from '../../server/defaultData.ts';

export const fallbackSiteSettings = initialSiteSettings;
export const fallbackHeroSlides = initialHeroSlides;
export const fallbackProjects = initialProjects;
export const fallbackServices = initialServices;
export const fallbackLocations = initialLocations;
export const fallbackClients = initialClients;
export const fallbackTestimonials = initialTestimonials;
export const fallbackLeaders = initialLeaders;
export const fallbackCareers = initialCareers;
export const fallbackDocuments = initialBrochures;
export const fallbackMedia = initialMedia;
export const fallbackQuotations = initialQuotations;
export const fallbackContacts = initialContacts;
export const fallbackApplications = initialApplications;
